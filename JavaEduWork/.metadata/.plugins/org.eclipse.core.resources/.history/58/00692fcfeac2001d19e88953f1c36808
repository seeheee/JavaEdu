package com.edu.vo.service;

import com.edu.vo.Product;

//상품들을 관리하는 기능들로만 작성된 서비스 클래스
// DB연동될 시 DAO 클래스로 발전
// 모든 함수는 전부 return 하도록 작성

// Method overrloading
// 하는 일은 같지만 처리하는 데이터를 달리할떄 사용하는 기법
// 1. 메소드 이름은 같되 인자값은 반드시 달라야한다(데이터타입, 갯수)
// 2. 메소드의 리턴타입은 상관없다.

/*
 * level3
 */

public class ProductService {
	
	public String[] getAllMaker(Product[] pros) {
		String[] temp = new String[pros.length];
		
		return temp;
	}
	
	public int getTotalPrice(Product[] pros) {
		
	}
	
	//1. 특정한 회사재품을 검색
	public String[] findCompany(Product[] pros) {
		
	}
	
	//2. 특정한 상품명을 검색
	public String[] findMaker(Product[] pros) {
		
	}
	
	//3. 특정한 가격대의 상품을 받아옴
	public int getPrice(Product[] pros) {
		
	}
	
	//4. 특정 가격 이상의 제품을
	public int getSpecificPrice(Product[] pros) {
		
	}
	
	//5. 구입한 상품들의 평균 단가
	public int getAveragePrice(Product[] pros) {
		
	}
	
	
	//  특정 가격 이상의 제품들을 가져옵니다.
	public Product[ ] getMorePrice(Product[ ] pros, int price) {
	    Product[ ] temp = new Product[pros.length];
	    for(Product p : temp) {
	    	p.getMaker();
	    }
	    return temp;
	}

	//  동일한 회사의 제품을 가져옵니다.
	public Product[ ] getCompany(Product[ ] pros, String company) {
	    Product[ ] temp = new Product[pros.length];
	    
	    return temp;
	}

	// 구매한 제품들의 평균가를 받아옵니다.
	public int getAvgPrice(Product[ ] pros) {
	    int avg = 0;
	    
	    return avg;
	}
	
	
}
